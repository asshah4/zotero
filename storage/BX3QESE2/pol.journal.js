//currentJournalNavFile = jQuery.url().attr("relative");
$(document).ready(function () {
/*
    $(".siteSubHead ul li").each(function () {
        if ($("a", this).attr("href") == currentJournalNavFile) {
            $(this).addClass("selected");
        }
    });
*/
});